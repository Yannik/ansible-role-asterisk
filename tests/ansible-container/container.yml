version: "2"
settings:
  conductor:
    base: debian:jessie
services:
  asterisk:
    from: debian:jessie
    roles:
      - pre
      - Yannik.asterisk
      - post
    command: ["/usr/sbin/asterisk", "-f"]
